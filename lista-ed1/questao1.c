#include <stdio.h>
#include <stdlib.h>


typedef struct item{
	int key;
}ITEM;

typedef struct pilha_estatica{
	int top;
}PILHA_ESTATICA;

A 0001
B 1010
C 1111

0001

{
	c = fread()
	int bin = tabela(c);
	 (bin){
	 	while(){
	 		bit = bin/10;
	 		totalbits = 0;
	 		if(){
	 			break;
	 		}else{
	 			x<<1
	 			x |= 0x1;
	 			totalbits++;
	 			if(totalbits == 8){
	 				fwrite(saida,x);
	 			}
	 		}
	 	}
	}
	0101010101 = "B";
	INT valor = b;
	bin = bin(valor);
	tabela 
	fwrite()
}

buffer << 1

if(bit)
	buffer |= 0x1; 